# Scripts and Batch Jobs

This directory contains all shell scripts (.sh), SLURM batch files (.sbatch), and environment configuration files (.yml) for the project.

- Use `.sh` files for shell automation and setup.
- Use `.sbatch` files for SLURM job submission.
- Use `.yml` files for environment configuration (e.g., Conda).

Move or add new scripts here to keep the project root organized.